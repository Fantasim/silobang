// =============================================================================
// DATA TABLE CONSTANTS
// =============================================================================

// Batch size for infinite scroll (rows loaded at a time)
export const DATA_TABLE_BATCH_SIZE = 50;

// Scroll threshold for loading more rows (pixels from bottom)
export const DATA_TABLE_SCROLL_THRESHOLD = 200;
